set -g default-terminal "st-256color"

# theme
######################
### DESIGN CHANGES ###
######################

# loud or quiet?
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

#  modes
setw -g clock-mode-colour colour5
setw -g mode-style 'fg=color1 bg=color8 bold'

# panes
set -g pane-border-style 'fg=color8 bg=default'
set -g pane-active-border-style 'bg=default fg=colour10'

# statusbar
set -g status-position bottom 
set -g status-justify centre
set -g status-style 'bg=color8 fg=color15'
set -g status-left '#[fg=color10,bg=colour8][#{b:pane_current_path}] #[fg=color15,bg=color8]>> #[fg=color11,bg=color8][#S] #[fg=color12,bg=color8][#I]'
set -g status-right '#[fg=color12,bg=color8][#(whoami)@#H] #[fg=color15,bg=color8]<<#[fg=color11,bg=color8] [%d/%m] #[fg=color10,bg=colour8][%H:%M:%S]'
set -g status-right-length 50
set -g status-left-length 20

setw -g window-status-current-style 'fg=colour8 bg=colour9 bold'
setw -g window-status-current-format ' #I#[fg=color8]:#[fg=color8]#W#[fg=color8]#F '

setw -g window-status-style 'fg=colour12 bg=colour8'
setw -g window-status-format ' #I#[fg=color12]:#[fg=color12]#W#[fg=color12]#F '






# Some settings
set -g prefix C-Space
set -g mouse on

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

# keys to resize panes
bind -r C-j resize-pane -D 5
bind -r C-k resize-pane -U 5
bind -r C-h resize-pane -L 5
bind -r C-l resize-pane -R 5

# window switcher
bind h previous-window
bind l next-window

bind -n M-j send-keys 'down'
bind -n M-k send-keys 'up'
bind -n M-h send-keys 'left'
bind -n M-l send-keys 'right'

# prefix l to clear 
bind M-b send-keys 'C-l'

set -s escape-time 0
