#!/bin/bash

if [[ ! $TMUX ]]; then
  exit
fi

dir=$(find $HOME/Projects/ -maxdepth 1 -type d | sed "s|$HOME/Projects/||g" | awk NF | fzf \
  --layout=reverse-list \
  ) || exit
tmux send-keys "cd $HOME/Projects/$dir" C-m
tmux send-keys "clear" C-m 
